// router/index.js - Completar
import { createRouter, createWebHistory } from 'vue-router'

const routes = [
    // Adicionar rotas aqui
]

const router = createRouter({
    history: createWebHistory(),
    routes
})

export default router